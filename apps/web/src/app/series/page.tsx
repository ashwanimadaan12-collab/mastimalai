import { BrowseGrid } from "@/components/BrowseGrid";

export const metadata = { title: "Web Series" };

export default function SeriesPage() {
  return <BrowseGrid kind="series" title="Web Series" />;
}
